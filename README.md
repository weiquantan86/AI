Genetic Algorithm that creates and trains creatures to climb a mountain. It supports a multithreaded simulation run on macbook, must downgrade to python 3.7

To recreate the environment, use conda env create -f environment.yml. 
